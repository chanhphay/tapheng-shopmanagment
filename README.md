# Tapheng Shop Management

ระบบจัดการร้านค้า สำหรับจัดการสินค้า สต็อก และการขาย

## การติดตั้ง

### 1. ติดตั้ง dependencies:

```bash
# npm
npm install

# pnpm
pnpm install

# yarn
yarn install

# bun
bun install
```

### 2. ตั้งค่า Supabase Database

ไปที่ Supabase Dashboard > SQL Editor และรัน SQL จากไฟล์ `supabase-schema.sql`:

```sql
-- สร้าง extension สำหรับ uuid
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- สร้างตาราง products และ product_variants
-- (ดูโค้ดเต็มในไฟล์ supabase-schema.sql)
```

### 3. ตรวจสอบไฟล์ .env

ตรวจสอบว่าไฟล์ `.env` มีค่าต่อไปนี้:
```
NUXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NUXT_PUBLIC_SUPABASE_KEY=your-anon-key
```

## โครงสร้างฐานข้อมูล

### ตาราง `products` (สินค้าหลัก)
- `id`: UUID (Primary Key)
- `name`: ชื่อสินค้า - เช่น 'Body Suit แขนสั้น'
- `base_cost`: ต้นทุนตัวเปล่า (NUMERIC)
- `base_price`: ราคาขาย (NUMERIC)
- `created_at`: วันที่สร้าง (TIMESTAMP)

### ตาราง `product_variants` (สี/รุ่นย่อย)
- `id`: UUID (Primary Key)
- `product_id`: อ้างอิงไปยัง products (UUID, Foreign Key)
- `color`: สี/รุ่น - เช่น 'ขาวขอบแดง', 'ฟ้า'
- `stock_qty`: จำนวนในสต็อก (INTEGER)
- `updated_at`: วันที่อัพเดทล่าสุด (TIMESTAMP, auto-update)

## การใช้งาน

### หน้าจัดการสินค้า (`/`)
- ➕ เพิ่มสินค้าใหม่
- ✏️ แก้ไขข้อมูลสินค้า
- 🗑️ ลบสินค้า
- 📊 ดูรายการสินค้าทั้งหมด
- 💰 คำนวณกำไร (ราคาขาย - ต้นทุน)
- 📦 จัดการสี/สต็อกของแต่ละสินค้า

### หน้าจัดการสี/สต็อก (`/products/:id/variants`)
- ➕ เพิ่มสี/รุ่นใหม่
- ✏️ แก้ไขสี/รุ่นและจำนวนสต็อก
- 🗑️ ลบสี/รุ่น
- 📊 ดูสถานะสต็อก:
  - 🟢 พร้อมขาย (≥ 5 ชิ้น)
  - 🟡 ใกล้หมด (1-4 ชิ้น)
  - 🔴 หมดสต็อก (0 ชิ้น)
- 📈 สรุปสต็อกรวมทั้งหมด

## Development Server

Start the development server on `http://localhost:3000`:

```bash
# npm
npm run dev

# pnpm
pnpm dev

# yarn
yarn dev

# bun
bun run dev
```

## Production

Build the application for production:

```bash
# npm
npm run build

# pnpm
pnpm build

# yarn
yarn build

# bun
bun run build
```

Locally preview production build:

```bash
# npm
npm run preview

# pnpm
pnpm preview

# yarn
yarn preview

# bun
bun run preview
```

## เทคโนโลยีที่ใช้

- **Nuxt 4** - Vue.js Framework
- **Vue 3** - Progressive JavaScript Framework
- **Supabase** - Backend as a Service (Database, Auth)
- **@nuxtjs/supabase** - Supabase module for Nuxt
- **TypeScript** - Type-safe JavaScript

## คุณสมบัติ

✅ จัดการสินค้าหลัก (บอดี้สูท, หมวก, ผ้ากันเปื้อน)  
✅ จัดการสี/รุ่นย่อยของแต่ละสินค้า  
✅ ติดตามสต็อกสินค้า  
✅ คำนวณกำไรอัตโนมัติ  
✅ แจ้งเตือนสต็อกใกล้หมด  
✅ Responsive Design (รองรับมือถือ)  

## หมายเหตุ

- ระบบใช้ Supabase เป็น Backend ดังนั้นต้องมี internet connection
- ข้อมูลทั้งหมดถูกเก็บบน Supabase Cloud
- การลบสินค้าหลักจะลบสี/รุ่นย่อยทั้งหมดที่เกี่ยวข้อง (CASCADE DELETE)
